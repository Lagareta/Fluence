<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fluencetest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fluencetest">
        <activity android:name=".FinalActivity"></activity>
        <activity android:name=".MenuExercice1" />
        <activity android:name=".Exercice1Activity" />
        <activity android:name=".ValiderActivity" />
        <activity android:name=".Exercice1" />
        <activity android:name=".ActivityBibliotheque" />
        <activity android:name=".ActivityExercices" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>